package edu.cyclone.insider.models;

public enum UserLevel {
    /**
     * generates the datatypes of user,professor, and admin for permissions.
     */
    USER,
    PROFESSOR,
    ADMIN,
    NONE;
}
